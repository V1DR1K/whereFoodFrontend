type Props={value:number;onChange?:(value:number)=>void;label:string}; export function StarRating({value,onChange,label}:Props){return <div className="star-control" role={onChange?'radiogroup':undefined} aria-label={label}>{[1,2,3,4,5].map(n=><button key={n} type="button" className={n<=value?'filled':''} disabled={!onChange} aria-label={`${n} estrellas`} aria-checked={n===value} onClick={()=>onChange?.(n)}>★</button>)}</div>}
