import { api } from '../../lib/api'; import type { Item,Slice } from '../../types/domain'; export type ItemInput={name:string;comment?:string;taste:number;price:number;venue:number}; export const getItems=(placeId:number,cursor?:number)=>api<Slice<Item>>(`/places/${placeId}/items?size=12${cursor?`&cursor=${cursor}`:''}`); export const saveItem=(placeId:number,input:ItemInput,id?:number)=>api<Item>(id?`/items/${id}`:`/places/${placeId}/items`,{method:id?'PUT':'POST',body:JSON.stringify(input)}); export const uploadPhoto=(id:number,file:File)=>{const data=new FormData();data.append('file',file);return api<Item>(`/items/${id}/photo`,{method:'POST',body:data})};
