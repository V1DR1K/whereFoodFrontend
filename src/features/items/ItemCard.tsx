import type { Item } from '../../types/domain'; import { StarRating } from '../../components/ui/StarRating';
export function ItemCard({item,onEdit,canEdit}:{item:Item;onEdit:(item:Item)=>void;canEdit:boolean}){return <article className="item-card">{item.thumbnailUrl&&<img src={item.thumbnailUrl} alt={item.name}/>}<div><div className="item-card-heading"><div><h3>{item.name}</h3><p className="byline">Reseña de {item.author}</p></div>{canEdit&&<button className="icon-edit" onClick={()=>onEdit(item)} aria-label={`Editar ${item.name}`}>✎</button>}</div>{item.comment&&<p>{item.comment}</p>}<div className="item-scores"><span>Sabor <StarRating label="Sabor" value={item.taste}/></span><span>Precio <StarRating label="Precio" value={item.price}/></span><span>Lugar <StarRating label="Lugar" value={item.venue}/></span></div></div></article>}
