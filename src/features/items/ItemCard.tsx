import type { Item } from '../../types/domain'; import { StarRating } from '../../components/ui/StarRating'; export function ItemCard({item}:{item:Item}){return <article className="item-card">{item.thumbnailUrl&&<img src={item.thumbnailUrl} alt={item.name}/>}<div><h3>{item.name}</h3><p className="byline">Reseña de {item.author}</p>{item.comment&&<p>{item.comment}</p>}<div className="item-scores"><span>Sabor <StarRating label="Sabor" value={item.taste}/></span><span>Precio <StarRating label="Precio" value={item.price}/></span><span>Lugar <StarRating label="Lugar" value={item.venue}/></span></div></div></article>}
