import { useInfiniteQuery,useQuery } from '@tanstack/react-query'; import { useState } from 'react'; import { Modal } from '../../components/ui/Modal'; import { LoadMore } from '../../components/ui/Pagination'; import type { PlaceStatus } from '../../types/domain'; import { getCategories } from '../categories/categories'; import { getHighlightTags } from './highlightTags'; import { PlaceCard } from './PlaceCard'; import { PlaceForm } from './PlaceForm'; import { getPlaces } from './places';
type FilterOption={id:number;label:string};
function FoodFilterChips({label,allLabel,options,value,onChange}:{label:string;allLabel:string;options:FilterOption[];value?:number;onChange:(value?:number)=>void}){const[showMore,setShowMore]=useState(false);const selected=(option?:FilterOption)=>option?option.id===value:!value;const choose=(option?:FilterOption)=>{onChange(option?.id);setShowMore(false)};return <section className="food-filter" aria-label={`Filtrar por ${label.toLowerCase()}`}><span>{label}</span><div className="chips"><button className={selected()?'selected':''} onClick={()=>choose()}>{allLabel}</button>{options.slice(0,5).map(option=><button key={option.id} className={selected(option)?'selected':''} onClick={()=>choose(option)}>{option.label}</button>)}{options.length>5&&<button className="food-filter-more" onClick={()=>setShowMore(true)} aria-label={`Ver más ${label.toLowerCase()}`}>•••</button>}</div>{showMore&&<Modal onClose={()=>setShowMore(false)}><p className="eyebrow">FILTRAR POR {label.toUpperCase()}</p><h2>Elegí una opción</h2><div className="chips food-filter-dialog"><button className={selected()?'selected':''} onClick={()=>choose()}>{allLabel}</button>{options.map(option=><button key={option.id} className={selected(option)?'selected':''} onClick={()=>choose(option)}>{option.label}</button>)}</div></Modal>}</section>}
function PlaceSection({status,category,highlightTagId,title,eyebrow,empty}:{status:PlaceStatus;category?:number;highlightTagId?:number;title:string;eyebrow:string;empty:string}){const query=useInfiniteQuery({queryKey:['places',status,category,highlightTagId],queryFn:({pageParam})=>getPlaces(category,pageParam,status,highlightTagId),initialPageParam:undefined as number|undefined,getNextPageParam:last=>last.nextCursor??undefined});const list=query.data?.pages.flatMap(p=>p.content)??[];return <section className="place-section"><div className="section-title"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2></div><strong>{list.length} lugares</strong></div>{query.isError?<p className="form-error">{query.error.message}</p>:list.length?<div className="place-grid">{list.map(p=><PlaceCard place={p} key={p.id}/>)}</div>:!query.isLoading&&<p className="empty-state">{empty}</p>}<LoadMore enabled={query.hasNextPage} onClick={()=>query.fetchNextPage()} loading={query.isFetchingNextPage}/></section>}
export function DiscoverPage(){const[category,setCategory]=useState<number>();const[highlightTagId,setHighlightTagId]=useState<number>();const[showForm,setShowForm]=useState(false);const categories=useQuery({queryKey:['categories'],queryFn:getCategories});const tags=useQuery({queryKey:['highlight-tags'],queryFn:getHighlightTags});return <><section className="hero"><div><p className="eyebrow">TU MAPA DEL HAMBRE</p><h1>¿Qué vamos a<br/><em>probar</em> hoy?</h1><p>Tu ranking personal de lugares que sí dan ganas de volver.</p></div><div className="hero-art">🍜<span>✦</span><b>🍗</b></div></section><nav className="quick-nav quick-nav-action"><button className="add-place-button" onClick={()=>setShowForm(true)}><span className="add-place-icon">＋</span><span><small>NUEVO PENDIENTE</small>Agendar lugar</span><b>📌</b></button></nav><section className="food-controls"><FoodFilterChips label="Categorías" allLabel="🍽️ Todos" options={(categories.data??[]).map(category=>({id:category.id,label:`${category.icon} ${category.name}`}))} value={category} onChange={setCategory}/><FoodFilterChips label="¿Por qué se destaca?" allLabel="Todos" options={(tags.data??[]).map(tag=>({id:tag.id,label:`${tag.emoji} ${tag.name}`}))} value={highlightTagId} onChange={setHighlightTagId}/></section><PlaceSection status="PENDING" category={category} highlightTagId={highlightTagId} eyebrow="POR PROBAR" title="Pendientes para ir" empty="Todavía no agendaste ningún lugar."/><PlaceSection status="REVIEWED" category={category} highlightTagId={highlightTagId} eyebrow="YA PROBAMOS" title="Reseñas hechas" empty="Cuando califiques tu primer pedido, aparecerá acá."/>{showForm&&<PlaceForm onClose={()=>setShowForm(false)}/>}</>}
