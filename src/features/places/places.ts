import { api } from '../../lib/api'; import type { Place,PlaceReview,PlaceStatus,Slice } from '../../types/domain'; export type PlaceInput={name:string;address?:string;sourceUrl?:string;mapsUrl?:string;categoryId:number}; export type PlaceReviewInput=Omit<PlaceReview,'author'>; export const getPlaces=(categoryId?:number,cursor?:number,status?:PlaceStatus)=>api<Slice<Place>>(`/places?size=12${categoryId?`&categoryId=${categoryId}`:''}${cursor?`&cursor=${cursor}`:''}${status?`&status=${status}`:''}`); export const getPlace=(id:number)=>api<Place>(`/places/${id}`); export const savePlace=(input:PlaceInput,id?:number)=>api<Place>(`/places${id?`/${id}`:''}`,{method:id?'PUT':'POST',body:JSON.stringify(input)}); export const savePlaceReview=(id:number,input:PlaceReviewInput)=>api<PlaceReview>(`/places/${id}/review`,{method:'PUT',body:JSON.stringify(input)}); export const uploadPlacePhoto=(id:number,file:File)=>{const data=new FormData();data.append('file',file);return api<Place>(`/places/${id}/photo`,{method:'POST',body:data})}; export const deletePlace=(id:number)=>api<void>(`/places/${id}`,{method:'DELETE'});
