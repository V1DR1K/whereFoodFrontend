import { Link } from 'react-router-dom'; import type { Place } from '../../types/domain'; import { StarRating } from '../../components/ui/StarRating'; export function PlaceCard({place}:{place:Place}){return <article className="place-card"><div className="food-poster"><span>{place.category.icon}</span><small>{place.zone||'Sin zona'} 📍</small></div><div className="card-body"><div className="card-top"><div><p>{place.category.name}</p><h3>{place.name}</h3></div><b>{place.rating.toFixed(1)} <span>★</span></b></div><div className="rating-preview"><span>Promedio</span><StarRating label="Puntuación promedio" value={Math.round(place.rating)}/></div><p className="note">Añadido por {place.author}</p><footer><span>💬 {place.itemCount} ítems</span><Link to={`/places/${place.id}`}>Ver ficha →</Link></footer></div></article>}
