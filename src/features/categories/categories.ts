import { api } from '../../lib/api'; import type { Category } from '../../types/domain'; export const getCategories=()=>api<Category[]>('/categories'); export const getAllCategories=()=>api<Category[]>('/categories/all'); export const saveCategory=(input:Omit<Category,'id'>,id?:number)=>api<Category>(`/categories${id?`/${id}`:''}`,{method:id?'PUT':'POST',body:JSON.stringify(input)});
